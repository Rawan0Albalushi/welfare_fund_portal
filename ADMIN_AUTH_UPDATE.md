# تحديث نظام المصادقة للإداريين

## ✅ التحديثات المطبقة

تم تحديث البورتال بالكامل لاستخدام نظام المصادقة الجديد للإداريين:

### 1. أنواع البيانات المحدثة
- **LoginRequest**: يستخدم `email` بدلاً من `phone`
- **LoginResponse**: يحتوي على `admin` بدلاً من `user`
- **AdminUser**: نوع جديد للإداريين مع `role` و `permissions`

### 2. خدمة المصادقة المحدثة
- **Login**: `/admin/auth/login` مع email/password
- **Logout**: `/admin/auth/logout`
- **Profile**: `/admin/auth/profile` للحصول على وتحديث الملف الشخصي

### 3. صفحة تسجيل الدخول المحدثة
- استخدام **البريد الإلكتروني** بدلاً من رقم الهاتف
- رسائل خطأ محسنة باللغة العربية
- معلومات تشخيصية للـ API URL

### 4. Hooks المصادقة المحدثة
- دعم `AdminUser` بدلاً من `User`
- إضافة `updateProfile` function
- الحفاظ على التوافق مع الكود الموجود

## 🔧 بيانات الاختبار

### بيانات تسجيل الدخول:
```
Super Admin:
- Email: admin@studentwelfarefund.com
- Password: admin123456

Admin عادي:
- Email: admin@example.com
- Password: admin123456
```

## 📋 خطوات التشغيل

### 1. إعداد البيئة
```bash
# إنشاء ملف .env
VITE_API_URL=http://localhost:8000/api
VITE_APP_NAME=Student Welfare Fund Admin Portal
```

### 2. تشغيل التطبيق
```bash
npm run dev
```

### 3. تسجيل الدخول
- استخدم البريد الإلكتروني وكلمة المرور
- تأكد من أن الخادم يعمل على العنوان الصحيح

## 🔍 التشخيص

### لوحة المعلومات التشخيصية
- تعرض API URL المستخدم
- حالة تحميل البيانات
- وجود Token والمستخدم

### Console Logs
- رسائل مفصلة لجميع عمليات المصادقة
- تفاصيل الأخطاء مع رموز الحالة
- معلومات عن الطلبات والاستجابات

## 🚨 معالجة الأخطاء

### رسائل الخطأ المحسنة:
- **401**: "بيانات الدخول غير صحيحة. تحقق من البريد الإلكتروني وكلمة المرور."
- **422**: "البيانات المدخلة غير صحيحة. تحقق من صحة البيانات."
- **Network Error**: "لا يمكن الاتصال بالخادم. تأكد من أن الخادم يعمل."
- **500+**: "خطأ في الخادم. حاول مرة أخرى لاحقاً."

## 🔄 API Endpoints

### المصادقة:
- `POST /admin/auth/login` - تسجيل الدخول
- `POST /admin/auth/logout` - تسجيل الخروج
- `GET /admin/auth/profile` - الحصول على الملف الشخصي
- `PUT /admin/auth/profile` - تحديث الملف الشخصي

### البيانات:
- `GET /v1/admin/dashboard/stats` - إحصائيات الداشبورد
- `GET /v1/admin/donations` - التبرعات
- `GET /v1/admin/applications` - الطلبات

## 🎯 الميزات الجديدة

1. **نظام أدوار**: Super Admin و Admin عادي
2. **صلاحيات**: نظام صلاحيات مرن
3. **تحديث الملف الشخصي**: إمكانية تحديث بيانات الإداري
4. **معالجة أخطاء محسنة**: رسائل واضحة ومفيدة
5. **تشخيص متقدم**: معلومات مفصلة عن حالة النظام

## 📝 ملاحظات مهمة

- **منفصل عن المستخدمين العاديين**: نظام Admin منفصل تماماً
- **Token-based Auth**: استخدام Bearer tokens
- **Protected Routes**: حماية جميع مسارات الإدارة
- **Auto Logout**: تسجيل خروج تلقائي عند انتهاء صلاحية Token
- **Backward Compatibility**: الحفاظ على التوافق مع الكود الموجود

## 🚀 الخطوات التالية

1. **تأكد من تشغيل الخادم** على العنوان الصحيح
2. **استخدم بيانات الاختبار** المذكورة أعلاه
3. **تحقق من console logs** لفهم أي مشاكل
4. **اختبر جميع الوظائف** للتأكد من عملها بشكل صحيح

البورتال الآن جاهز للعمل مع نظام المصادقة الجديد للإداريين! 🎉

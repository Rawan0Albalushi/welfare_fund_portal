# دليل حل مشكلة Network Error

## المشكلة
عند محاولة تحميل البيانات، يظهر خطأ "Network Error" مما يعني أن التطبيق لا يستطيع الاتصال بالخادم.

## الأسباب المحتملة

### 1. الخادم غير شغال
- تأكد من أن الخادم (Laravel Backend) يعمل
- افتح Terminal واذهب لمجلد الباكند
- شغّل الأمر:
```bash
php artisan serve --host=0.0.0.0 --port=8000
```

### 2. عنوان API خاطئ
العنوان الحالي في معلومات الخطأ: `http://192.168.100.105:8000/api/v1/admin`

#### الحل:
1. أنشئ ملف `.env` في جذر المشروع (نفس المكان الذي فيه `package.json`)
2. اكتب فيه:
```env
# للتطوير المحلي
VITE_API_URL=http://localhost:8000/api/v1/admin

# أو إذا كان الخادم على جهاز آخر في الشبكة المحلية
VITE_API_URL=http://192.168.1.21:8000/api/v1/admin

# Application Name
VITE_APP_NAME=Student Welfare Fund Admin Portal
```

3. **مهم جداً**: أوقف التطبيق (Ctrl+C) وأعد تشغيله:
```bash
npm run dev
```

### 3. مشاكل CORS
إذا كان الخادم يعمل لكن المشكلة مستمرة، قد تكون مشكلة CORS في الباكند.

#### الحل في Laravel:
1. افتح ملف `config/cors.php` في الباكند
2. تأكد من الإعدادات:
```php
'paths' => ['api/*', 'sanctum/csrf-cookie'],
'allowed_origins' => ['http://localhost:5173', 'http://127.0.0.1:5173'],
'allowed_methods' => ['*'],
'allowed_headers' => ['*'],
'supports_credentials' => true,
```

3. أو في ملف `.env` في الباكند:
```env
SANCTUM_STATEFUL_DOMAINS=localhost:5173,127.0.0.1:5173
SESSION_DOMAIN=localhost
```

### 4. جدار ناري
- تأكد من عدم وجود Firewall يحجب المنفذ 8000
- في Windows Defender Firewall، سمح بالاتصال على المنفذ 8000

## التحقق السريع

### تحقق من الخادم
افتح المتصفح واذهب لـ:
```
http://localhost:8000/api/v1/admin/stats
```
- إذا ظهر JSON response = الخادم يعمل ✅
- إذا ظهر "Connection refused" = الخادم غير شغال ❌

### تحقق من Token
افتح Console في المتصفح (F12) واكتب:
```javascript
localStorage.getItem('admin_token')
```
- إذا ظهر token = موجود ✅
- إذا ظهر null = تحتاج تسجيل دخول ❌

## الخطوات المقترحة بالترتيب

1. ✅ **شغّل الخادم**:
```bash
cd path/to/backend
php artisan serve --host=0.0.0.0
```

2. ✅ **أنشئ ملف .env**:
```env
VITE_API_URL=http://localhost:8000/api/v1/admin
VITE_APP_NAME=Student Welfare Fund Admin Portal
```

3. ✅ **أعد تشغيل التطبيق**:
```bash
# أوقف التطبيق (Ctrl+C)
npm run dev
```

4. ✅ **تحقق من CORS في الباكند**
5. ✅ **تحقق من الـ Firewall**

## معلومات إضافية

### إذا كان الخادم على جهاز آخر
إذا كان الباكند يعمل على جهاز آخر في الشبكة (مثلاً `192.168.100.105`):

1. تأكد من أن الخادم يعمل بـ `--host=0.0.0.0`:
```bash
php artisan serve --host=0.0.0.0 --port=8000
```

2. غيّر عنوان API في `.env`:
```env
VITE_API_URL=http://192.168.100.105:8000/api/v1/admin
```

3. تأكد من إضافة عنوان الفرونت إند في CORS في الباكند:
```php
'allowed_origins' => [
    'http://localhost:5173',
    'http://192.168.100.105:5173', // عنوان جهازك
],
```

### استكشاف الأخطاء المتقدم

افتح Console في المتصفح (F12) وتابع الرسائل:
- `🌐 [API] Request:` - الطلبات المرسلة
- `✅ [API] Response:` - الردود الناجحة
- `❌ [API] Error:` - الأخطاء

## الدعم

إذا استمرت المشكلة بعد تطبيق جميع الحلول، تحقق من:
1. Console في المتصفح للمزيد من المعلومات
2. Logs في الباكند: `storage/logs/laravel.log`
3. Network tab في Developer Tools لمعرفة حالة الطلبات


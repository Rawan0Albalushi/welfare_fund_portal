# 🔧 حل مشكلة الاتصال بالخادم (Network Error)

## ⚡ الحل السريع

### الخطوة 1: شغّل الخادم
افتح Terminal في مجلد الباكند واكتب:
```bash
php artisan serve --host=0.0.0.0
```

### الخطوة 2: أنشئ ملف .env
في مجلد المشروع (welfare-fund-portal)، أنشئ ملف اسمه `.env` واكتب فيه:

```env
VITE_API_URL=http://localhost:8000/api/v1/admin
VITE_APP_NAME=Student Welfare Fund Admin Portal
```

**ملاحظة مهمة**: إذا كان الخادم على جهاز آخر (مثلاً `192.168.100.105`):
```env
VITE_API_URL=http://192.168.100.105:8000/api/v1/admin
```

### الخطوة 3: أعد تشغيل التطبيق
```bash
# أوقف التطبيق بالضغط على Ctrl+C
# ثم شغله مرة أخرى
npm run dev
```

---

## 📝 شرح المشكلة

حالياً، التطبيق يحاول الاتصال بـ:
```
http://192.168.100.105:8000/api/v1/admin/stats
```

ولكن الخادم إما:
- ❌ غير شغال
- ❌ على عنوان مختلف
- ❌ محجوب بواسطة Firewall

---

## ✅ كيف تتحقق من الخادم

### طريقة 1: افتح في المتصفح
اذهب لـ:
```
http://localhost:8000/api/v1/admin/stats
```
- إذا ظهر JSON → الخادم يعمل ✅
- إذا ظهر خطأ → الخادم متوقف ❌

### طريقة 2: تحقق من Console
اضغط F12 في المتصفح، واذهب لتبويب Console:
- ابحث عن رسائل `🌐 [API] Request`
- ابحث عن رسائل `❌ [API] Error`

---

## 🚀 خطوات إضافية للحل

### إذا استمرت المشكلة:

#### 1. تحقق من CORS في الباكند
افتح `config/cors.php` في الباكند وتأكد من:
```php
'allowed_origins' => ['http://localhost:5173'],
'supports_credentials' => true,
```

#### 2. تحقق من Token
افتح Console (F12) واكتب:
```javascript
localStorage.getItem('admin_token')
```
إذا كان `null`، سجّل دخول مرة أخرى.

#### 3. تحقق من Firewall
- في Windows: ابحث عن "Windows Defender Firewall"
- سمح بالاتصال على المنفذ 8000

---

## 📞 إذا لم تحل المشكلة

افتح Developer Tools (F12):
1. اذهب لتبويب **Network**
2. حاول تحميل الصفحة مرة أخرى
3. انظر للطلبات باللون الأحمر
4. اضغط عليها لمعرفة السبب

أو تحقق من logs الباكند:
```bash
cd path/to/backend
tail -f storage/logs/laravel.log
```

